# Musawo AI — Python dependencies (2026 production stable)

# Core API
fastapi>=0.115.0,<0.116
uvicorn[standard]>=0.32.0,<0.35
pydantic>=2.10.0,<2.12
pydantic-settings>=2.5,<3
python-multipart>=0.0.9

# SSE streaming
sse-starlette>=2.0.0

# DNS-over-HTTPS resolver (Crane Cloud / RENU egress workaround)
dnspython>=2.6,<3

# Data & retrieval
sentence-transformers>=3.4,<4
qdrant-client>=1.13,<2
numpy>=1.26,<3

# Voice — Silero VAD neural inference (CPU-only, ~40MB)
onnxruntime>=1.17.0,<2

# LLM — Gemini (default) + Groq (fallback), both via the OpenAI-compatible client
openai>=1.30.0

# LLM — Local fallback (Qwen3-8B)
transformers>=4.46.0
torch>=2.4.0
accelerate>=1.2.0

# SMS — Twilio
twilio>=9.0.0

# Observability
prometheus-client>=0.21.0

# Testing
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
