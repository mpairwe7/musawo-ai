# Musawo AI — Python dependencies (2026 production stable)

# Core API
fastapi>=0.115.0,<0.116
uvicorn[standard]>=0.32.0,<0.35
pydantic>=2.10.0,<2.12
python-multipart>=0.0.9

# SSE streaming
sse-starlette>=2.0.0

# Data & retrieval
sentence-transformers>=3.4,<4
qdrant-client>=1.13,<2
numpy>=1.26,<3

# LLM — Groq (free tier, OpenAI-compatible)
openai>=1.30.0

# LLM — Claude API
anthropic>=0.45.0

# LLM — Local fallback (Qwen3-8B)
transformers>=4.46.0
torch>=2.4.0
accelerate>=1.2.0

# SMS — Twilio
twilio>=9.0.0

# Observability
prometheus-client>=0.21.0

# Testing
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
